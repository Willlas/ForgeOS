# Prototype area

> **Maturity: Experimental** — explorations and mockups, explicitly not product. See `PROJECT_STATE.md` (`## Workstream Maturity Audit`).

Contents:

- `login-page-2026.html` — static login page mockup (design exploration, not wired to any build)

Rules: nothing here feeds the build, the CLI, or the runtime. Promote a prototype to `packages/` only when it is validated and in-scope for the MVP.