# Experiments

> **Maturity: Experimental** — throwaway validation runs and explorations, explicitly not product. See `PROJECT_STATE.md` (`## Workstream Maturity Audit`).

Contents:

- `console-validation.txt` — output of a manual `runtime apply` console validation

Rules: experiments do not feed the build or test pipeline. Keep each entry self-contained and disposable; promote findings to `docs/` (research) or `packages/` (code) when they matter.